# YREP Analysis

Repo for running spectral analysis on the supplied datasets using the local library.

## Setup (uv)

```bash
uv sync
```

## Run

```bash
uv run python run_with_library.py
```

Outputs will be written under `plots/`.
